"""
This type stub file was generated by pyright.
"""

import pandas as pd
from typing import Optional
from openbb_terminal.decorators import check_api_key, log_start_end

logger = ...
@log_start_end(log=logger)
@check_api_key(["API_FINNHUB_KEY"])
def get_ipo_calendar(start_date: Optional[str] = ..., end_date: Optional[str] = ...) -> pd.DataFrame:
    """Get IPO calendar

    Parameters
    ----------
    start_date : Optional[str]
        Initial date, format YYYY-MM-DD
    end_date : Optional[str]
        Final date, format YYYY-MM-DD

    Returns
    -------
    pd.DataFrame
        Get dataframe with IPO calendar events
    """
    ...

@log_start_end(log=logger)
@check_api_key(["API_FINNHUB_KEY"])
def get_past_ipo(num_days_behind: int = ..., start_date: Optional[str] = ...) -> pd.DataFrame:
    """Past IPOs dates. [Source: Finnhub]

    Parameters
    ----------
    num_days_behind: int
        Number of days to look behind for IPOs dates
    start_date: str
        The starting date (format YYYY-MM-DD) to look for IPOs

    Returns
    -------
    pd.DataFrame
        Get dataframe with past IPOs
    """
    ...

@log_start_end(log=logger)
def get_future_ipo(num_days_ahead: int = ..., end_date: Optional[str] = ...) -> pd.DataFrame:
    """Future IPOs dates. [Source: Finnhub]

    Parameters
    ----------
    num_days_ahead: int
        Number of days to look ahead for IPOs dates
    end_date: datetime
        The end date (format YYYY-MM-DD) to look for IPOs from today onwards

    Returns
    -------
    pd.DataFrame
        Get dataframe with future IPOs
    """
    ...

