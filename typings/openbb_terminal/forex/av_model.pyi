"""
This type stub file was generated by pyright.
"""

import pandas as pd
from typing import Any, Dict, List
from openbb_terminal.decorators import log_start_end

"""AlphaVantage FX Model."""
logger = ...
def get_currency_list() -> List:
    """Load AV currency codes from a local file."""
    ...

CURRENCY_LIST = ...
@log_start_end(log=logger)
def check_valid_forex_currency(symbol: str) -> str:
    """Check if given symbol is supported on alphavantage.

    Parameters
    ----------
    symbol : str
        Symbol to check

    Returns
    -------
    str
        Currency symbol

    Raises
    ------
    argparse.ArgumentTypeError
        Symbol not valid on alphavantage
    """
    ...

@log_start_end(log=logger)
def get_quote(to_symbol: str = ..., from_symbol: str = ...) -> Dict[str, Any]:
    """Get current exchange rate quote from alpha vantage.

    Parameters
    ----------
    to_symbol : str
        To forex symbol
    from_symbol : str
        From forex symbol

    Returns
    -------
    Dict[str, Any]
        Dictionary of exchange rate
    """
    ...

@log_start_end(log=logger)
def get_historical(to_symbol: str = ..., from_symbol: str = ..., resolution: str = ..., interval: int = ..., start_date: str = ..., end_date: str = ...) -> pd.DataFrame:
    """Get historical forex data.

    Parameters
    ----------
    to_symbol : str
        To forex symbol
    from_symbol : str
        From forex symbol
    resolution : str, optional
        Resolution of data.  Can be "i", "d", "w", "m" for intraday, daily, weekly or monthly
    interval : int, optional
        Interval for intraday data
    start_date : str, optional
        Start date for data.
    end_date : str, optional
        End date for data.

    Returns
    -------
    pd.DataFrame
        Historical data for forex pair
    """
    ...

