"""
This type stub file was generated by pyright.
"""

import pandas as pd
from typing import Any, Optional, Tuple
from openbb_terminal.decorators import log_start_end

"""Custom Indicator Models"""
__docformat__ = ...
logger = ...
@log_start_end(log=logger)
def calculate_fib_levels(data: pd.DataFrame, limit: int = ..., start_date: Optional[Any] = ..., end_date: Optional[Any] = ...) -> Tuple[pd.DataFrame, pd.Timestamp, pd.Timestamp, float, float, str]:
    """Calculate Fibonacci levels

    Parameters
    ----------
    data : pd.DataFrame
        Dataframe of prices
    limit : int
        Days to look back for retracement
    start_date : Any
        Custom start date for retracement
    end_date : Any
        Custom end date for retracement

    Returns
    -------
    df : pd.DataFrame
        Dataframe of fib levels
    min_date: pd.Timestamp
        Date of min point
    max_date: pd.Timestamp:
        Date of max point
    min_pr: float
        Price at min point
    max_pr: float
        Price at max point
    """
    ...

