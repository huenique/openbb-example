"""
This type stub file was generated by pyright.
"""

import pandas as pd
from datetime import datetime
from typing import Optional, Union
from openbb_terminal import OpenBBFigure
from openbb_terminal.decorators import log_start_end

"""Quantile Anomaly Detector View"""
__docformat__ = ...
logger = ...
@log_start_end(log=logger)
def display_anomaly_detection(data: Union[pd.Series, pd.DataFrame], dataset_name=..., target_column: str = ..., train_split: float = ..., export: str = ..., start_date: Optional[datetime] = ..., end_date: Optional[datetime] = ..., external_axes: bool = ...) -> Union[None, OpenBBFigure]:
    """Display Quantile Anomaly Detection

    Parameters
    ----------
    data: Union[pd.Series, pd.DataFrame]

    Input Data
    ----------
    target_column: str
        Target column to forecast. Defaults to "close".
    train_split: (float, optional)
        Train/val split. Defaults to 0.85.
    export: (str, optional)
        Export data to csv, jpg, png, or pdf. Defaults to "".
    start_date: (Optional[datetime], optional)
        Start date. Defaults to None.
    end_date: (Optional[datetime], optional)
        End date. Defaults to None.
    external_axes : bool, optional
        Whether to return the figure object or not, by default False

    Returns
    -------
    Union[None, go.Figure]
        None if external_axes is True, otherwise the figure object
    """
    ...

